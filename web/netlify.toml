[build]
  command = "npm install && npx prisma generate && npm run build"
  # Publish directory - Next.js plugin will handle routing, but Netlify UI requires a value
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

