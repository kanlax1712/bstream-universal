[build]
  command = "npm install && npx prisma generate && npm run build"
  # Don't set publish directory - Next.js plugin handles it automatically

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

